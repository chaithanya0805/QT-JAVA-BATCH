Internal Architecture of JVM 
============================
Diagram: class6.1

Java program contains java code instructions. Once if we compiled , our compiler converts java code instructions to byte code instructions in .class file. 

JVM invokes one module called classloader/subsystem to load all the byte code instructions from .class file. The work of classloader is to check these byte code instructions are proper or not. If they are not proper then it will refuse the execution. If they are proper then it will allocates memory.

We have five memories in java.

1) Method Area 
--------------
It contains code of a class, code of a variable and code of a method.

2) Heap 
--------
Our object creations will store in heap area.

3) Java Stack 
-------------
Java methods will store in method area but to execute those methods we required some memory and that memory is allocated in java stack.

4) PC Register 
------------
It is a program counter register which is used to track the address of an instructions. 

5) Native Method Stack 
---------------------
Java methods execute in Method area.
Similarly native methods execute in native method stack.
But to execute native methods we required a program called Native method interface.

Execution Engine 
---------------
Executine engine contains interpreter and JIT compiler.

Interpreter is used to execute our program line by line procedure.

JIT compiler is used to increase the execution speed of our program.

Whenever JVM loads byte code instructions from .class file , it simultenously uses interpreter and JIT compiler.

Finally, JVM converts byte code to machine code.


Interview Questions 
===================

Q) How many memories are there in java?

There are five memories in java.

1) Method Area 
2) Heap 
3) Java Stack 
4) PC Register 
5) Native Method Stack 


Q) What is JIT Compiler?

JIT compiler is a part of a JVM which is used to increase the execution speed of our program.


Q) What is native method in java?

A method which is developed by using some other language is called native method.
To execute native methods we required a program called native method interface.
All native methods store in native method stack memory.


Q) Is java platform dependent or independent?

Diagram: class6.2

Java is a platform independent at byte code level.
Hence java is also known as WORA (Writen Once Run Anywhere).


Q) Is JVM platform dependent or independent?

	JVM is platform dependent






















